class Solution {
    public boolean isPowerOfThree(int n) {
        int temp = n;
        double result =0;
        int i=0;
        while(result<=n){
            result = Math.pow(3,i);
            i++;

            if(n == result){
                return true;
            }
        }

        

        return false;
    }
}
